/* 给定两个矩形的左下角，右上角左边，求出这两个矩形的面积
 * Solution：Area_A + Area_B - overlap
 * overlap的计算是，算出left,right, bottom, up，观察图即可求出
 */

class Solution {
public:
    int computeArea(int A, int B, int C, int D, int E, int F, int G, int H) {
        
    }
};
