/* 给定一个array，和一个窗口大小k,窗口从左向右，求窗口滑动过程中，窗口中的最大值
 * Solution：窗口中始终保持可能的最大值，并不断的从前后对窗口的性质进行维护，剑指offer原题
 */

class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        
    }
};
