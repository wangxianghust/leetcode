/* 使用O(n*lg n)排序一个List，常数space
 * Solution： merge sort, 对list 取mid时候，使用slow 1 step，fast 2 step 的策略
 */

/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode(int x) : val(x), next(NULL) {}
 * };
 */
class Solution {
public:
    ListNode* sortList(ListNode* head) {
        
    }
};
