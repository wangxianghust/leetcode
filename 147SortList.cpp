/* 使用插入排序的方法排序一个list
 * Solution:1.如果对内存没有要求，将value拷贝出来，排序后再放回去
 * 2.对list纯操作的话，没有难度，设置一个dummy，然后每次寻找正确位置的时候都从dummy开始遍历
 */

/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode(int x) : val(x), next(NULL) {}
 * };
 */
class Solution {
public:
    ListNode* insertionSortList(ListNode* head) {
        
    }
};
