/* 给定一个数组，求连续数组的长度，求最长，要O(n)的时间
 * Solution时间要求，空间来凑，使用hash set. unordered_set
 * 每一查找一个元素，while(search pre--)set.erase(pre), while(search next++)set.rease(next);
 * 最长嘛，设置一个longest，记录最长的值。
 */

class Solution {
public:
    int longestConsecutive(vector<int>& nums) {
        
    }
};
