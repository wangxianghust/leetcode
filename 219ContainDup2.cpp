/* 判断是否存在两个相等的数字，且数字间距最多为k
 * Solution：用unordered_set进行判断有没有重复，同时维护该set的大小为k, 就是超过
 * 的时候，不断删除前面的元素。
 */

class Solution {
public:
    bool containsNearbyDuplicate(vector<int>& nums, int k) {
        
    }
};
