/* 判断一个链表是不是回文
 * 要求O(n) time, O(1) space
 * Solution: 找到中间的位置，将前半段reverse，再继续比较
 */

/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode(int x) : val(x), next(NULL) {}
 * };
 */
class Solution {
public:
    bool isPalindrome(ListNode* head) {
        
    }
};
