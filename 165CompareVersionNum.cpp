/*给定一个string,中间使用.进行分割，比较两个string的大小，越是在前面优先级越高，.是分割符。
 * Solution:如此无聊的题目，可以检测数字，将其直接变成int进行比价吧，可以AC。
 */

class Solution {
public:
    int compareVersion(string version1, string version2) {
        
    }
};
