/*模拟出发操作，将分数变成小数，对于循环的部分使用()扩起来，如 1/3 = 0.(3), 2/5 = 0.4, -10/2 = -5
 * Solution:先对符号，整数部分进行处理，然后模拟小数计算，使用hash_map记录除数的位置，一旦发现除数再次出现，则发现了循环体（患者除数
 * 为0），这两个都是终止条件。
 */

class Solution {
public:
    string fractionToDecimal(int numerator, int denominator) {
        
    }
};
