/* 有+ - *三种操作和加括号，求不同的加符号的方法。
 * Solution：采用分治法，每次求出(0,i)，(i,end)的结果，然后再根据符号判断，计算最后的结果
 * 改进：可以使用map记录  string 对应的 ret。避免重复计算。
 */

class Solution {
public:
    vector<int> diffWaysToCompute(string input) {
        
    }
};
