/* 设计一种新的stack，具有min函数，返回当前stack中的最小值，要求min, top, pop操作都是常数时间，
 * Solution：要求时间，空间来凑，再开一个stack,top是当前的最小值，所以新的stack在push时候要和top比较，pop两个stack直接pop即可。
 */

class MinStack {
public:
    /** initialize your data structure here. */
    MinStack() {
        
    }
    
    void push(int x) {
        
    }
    
    void pop() {
        
    }
    
    int top() {
        
    }
    
    int getMin() {
        
    }
};

/**
 * Your MinStack object will be instantiated and called as such:
 * MinStack obj = new MinStack();
 * obj.push(x);
 * obj.pop();
 * int param_3 = obj.top();
 * int param_4 = obj.getMin();
 */
