/* 在二维数组中对某个元素进行Search，该数组从左向右递增，从上向下递增
 * Solution：选择右上角元素，target > select, 舍去这一行，else 舍去这一列
 * 复杂度O(m+n)
 */

class Solution {
public:
    bool searchMatrix(vector<vector<int>>& matrix, int target) {
        
    }
};
